<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Calculater" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/black</item>
        <item name="colorPrimaryVariant">@color/black</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/orange_200</item>
        <item name="colorSecondaryVariant">@color/orange_200</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <!-- theme of background -->
    <style name="bacground">
        <item name="android:background">@color/black</item>
        <item name="android:padding">@dimen/padding_small</item>
    </style>
    <!-- theme of button in this calculator -->
    <style name="button_attr">
        <item name="android:textAllCaps">true</item>
        <item name="android:textSize">@dimen/text_size_normal</item>
        <item name="android:textStyle">bold</item>
    </style>
    <!-- theme of button that content the operations  -->
    <style name="button_attr.operation_attr">
        <item name="android:textColor">@color/orange_200</item>
    </style>
    <!-- theme of button that content the numbers  -->
    <style name="button_attr.numbers_attr">
        <item name="android:textColor">@color/orange_100</item>
    </style>
</resources>